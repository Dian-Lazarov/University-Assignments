	jmp start	; преход към осн. програма

	; Тестов масив
	db DA
	db 1C
	db 24
	db FA
	db 0B

	; Кодова таблица - ASCII кодове на 16-те цифри
	db "0123456789ABCDEF"

;----------------------------------------------------------
; Подпрограма за извеждане на една 16-на цифра
;----------------------------------------------------------
; Вх. аргументи:
; al - извеждана 16-на цифра
; cl - указател (адрес на извеждане във видеопамет)
dspl:	
    ; TODO:
	PUSH DL
	MOV DL, 07
	ADD DL, AL
	MOV AL, [DL]
	MOV [CL], AL
	POP DL
	RET


;----------------------------------------------------------
; Основна програма (опр. на макс. ел. от масив)
;----------------------------------------------------------
; Раб. регистри:
; al - адрес на i-тия елемент
; bl - макс. елемент
; cl - буфер

start:
	MOV DL, 02	; указател
	MOV BL, [DL] 	; зареждане на 1-ви елемент
loop:
	INC DL
	CMP DL, 07
	JZ finish
	MOV AL, [DL]
	CMP BL, AL
	JNS skip
	MOV BL, [DL]
skip:
	JMP loop
finish:
	PUSH BL
	POP AL 		; AL = BL
	SHR AL
	SHR AL
	SHR AL
	SHR AL
	MOV CL, C0
	CALL 17
	PUSH BL
	POP AL
	AND AL, 0F
	INC CL
	CALL 17

end