title Диаграма 2: Избор на игра и преглед на детайлен изглед

actor Потребител
participant "Frontend (UI)" as UI
participant "RAWG API" as API

Потребител->+UI: Кликва върху карта на избрана игра
UI->UI: Показва индикатор за зареждане (Loading...)

par Паралелно извличане на данни
    UI->+API: GET /games/{id} (основни детайли)
    UI->API: GET /games/{id}/screenshots (галерия)
end

API-->UI: Връща детайли (жанр, рейтинг, платформи...)
API-->-UI: Връща изображения (скрийншоти)

UI-->-Потребител: Изобразява пълната страница с детайли за играта